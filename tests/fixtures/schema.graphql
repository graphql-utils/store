type Post {
  id: ID!
  title: String!
  body: String!
}

type User {
  id: ID!
  username: String!
  posts: [Post]!
}

type Query {
  user(id: ID!): User
}

type Mutation {
  createUser(id: ID!): User
}
